# AWS S3 Configuration for Portfolio Storage

# Required - Your AWS Credentials
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1

# Required - Your S3 Bucket Name
S3_BUCKET_NAME=your-bucket-name

# Optional - Custom S3 Endpoint (for MinIO, DigitalOcean Spaces, etc.)
# S3_ENDPOINT=https://s3.us-east-1.amazonaws.com

# NextAuth Configuration (already set up)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret

# Admin Configuration (already set up)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD_HASH=$2b$12$...
