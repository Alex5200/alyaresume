# 🔓 Настройка публичного доступа к S3 бакету Selectel

## 🎯 Проблема

PDF файлы загружены в S3, но возвращают ошибку 403 (Forbidden) при попытке доступа.

## 🛠️ Решение 1: Настройка публичной политики бакета

### 1. Войдите в панель управления Selectel
1. Перейдите в [Панель управления Selectel](https://my.selectel.ru/)
2. Авторизуйтесь в своем аккаунте

### 2. Найдите ваш бакет
1. Перейдите в раздел "Объектное хранилище" → "S3"
2. Найдите бакет с именем `alyaadmin`
3. Нажмите на бакет

### 3. Настройте публичный доступ
1. Перейдите в "Настройки" или "Access Policy"
2. Установите одну из политик:

#### Вариант А: Публичное чтение (рекомендуется)
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadGetObject",
      "Effect": "Allow",
      "Principal": "*",
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::alyaadmin/*"
    }
  ]
}
```

#### Вариант Б: Полностью публичный бакет
```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicAccess",
      "Effect": "Allow",
      "Principal": "*",
      "Action": [
        "s3:GetObject",
        "s3:GetObjectVersion",
        "s3:ListBucket"
      ],
      "Resource": [
        "arn:aws:s3:::alyaadmin",
        "arn:aws:s3:::alyaadmin/*"
      ]
    }
  ]
}
```

### 4. Сохраните изменения
1. Нажмите "Сохранить" или "Apply"
2. Подождите несколько минут для применения политики

## 🛠️ Решение 2: Через CLI (если доступно)

```bash
# Установка AWS CLI
pip install awscli

# Настройка профиля
aws configure --profile selectel
# AWS Access Key ID: ваш_ключ
# AWS Secret Access Key: ваш_секрет
# Default region name: ru-3
# Default output format: json

# Применение политики
aws s3api put-bucket-policy --bucket alyaadmin --policy file://policy.json --profile selectel
```

## 🧪 Проверка доступа

После настройки проверьте доступность:

```bash
# Тест доступа к файлу
curl -I "https://s3.ru-3.storage.selcloud.ru/alyaadmin/portfolio/1771763902093_1.pdf"

# Ожидаемый результат: HTTP/1.1 200 OK
```

## 🔄 Возврат к S3 URL

После настройки публичного доступа:

1. **Обновите данные портфолио**:
```bash
# Откройте data/portfolio.json и замените локальные URL на S3:
{
  "projects": [
    {
      "pdfUrl": "https://s3.ru-3.storage.selcloud.ru/alyaadmin/portfolio/1771763902093_1.pdf"
    },
    {
      "pdfUrl": "https://s3.ru-3.storage.selcloud.ru/alyaadmin/portfolio/1771763903728_2.pdf"
    }
  ]
}
```

2. **Перезапустите dev сервер**:
```bash
npm run dev
```

3. **Проверьте работу сайта** - PDF должны загружаться из S3

## 🆘 Если не работает

### Проверьте:
1. **Правильность имени бакета** - должно быть `alyaadmin`
2. **Регион** - файлы в `ru-3` (Санкт-Петербург)
3. **Политику доступа** - должна содержать `s3:GetObject`
4. **SSL сертификат** - для `s3.ru-3.storage.selcloud.ru`

### Альтернативные домены:
- `https://s3.ru-3.storage.selcloud.ru/бакет/файл`
- `https://бакет.s3.ru-3.storage.selcloud.ru/файл`

## 💡 Дополнительные настройки

### CORS (если нужно для фронтенда)
```json
[
  {
    "AllowedHeaders": ["*"],
    "AllowedMethods": ["GET", "HEAD"],
    "AllowedOrigins": ["*"],
    "ExposeHeaders": ["ETag"]
  }
]
```

### Версионирование (для бэкапов)
Включите в настройках бакета для защиты от случайного удаления.

## 📊 Текущий статус

- ✅ Файлы загружены в S3
- ✅ API настроен на S3
- ✅ Локальные файлы работают как fallback
- ⏳ Ожидает настройки публичного доступа бакета

После настройки публичного доступа сайт будет полностью работать с S3!
