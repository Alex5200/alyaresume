# 🔐 Безопасная авторизация - Готовое решение

## 📋 Данные для входа:
- **Email:** `admin@example.com`
- **Пароль:** `admin123`

## 🚀 Быстрый старт:

### 1. Создайте .env.local файл:
```bash
ADMIN_EMAIL=admin@example.com
ADMIN_HASHED_PASSWORD=$2b$12$cmcgt1O9MtFtKcExuY6pxO0izceACKm0zfyo53CVHB8cZ.svYrx0K
NEXTAUTH_SECRET=Pg9+WvkwXc//pPbTawGm70UQ267FuEytxw4sjqbNwY4=
NEXTAUTH_URL=http://localhost:3000
```

### 2. Запустите проект:
```bash
npm run dev
```

### 3. Войдите в админ панель:
- Перейдите на `http://localhost:3000/admin/login`
- Введите email и пароль выше

## 🔧 Генерация нового пароля:

### Способ 1: Скрипт
```bash
node scripts/generate-password.js ваш_новый_пароль
```

### Способ 2: Вручную
```bash
node -e "const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync('ваш_пароль', 12));"
```

## 🛡️ Безопасность:

✅ **Хеширование паролей** - bcrypt с солью
✅ **Защита роутов** - middleware для всех /admin/* путей  
✅ **JWT сессии** - безопасное управление сессиями
✅ **Валидация email** - проверка формата и разрешенных адресов
✅ **OAuth поддержка** - Google OAuth как альтернатива
✅ **CSRF защита** - встроенная в NextAuth
✅ **Редиректы** - безопасные перенаправления

## 📁 Структура файлов:
- `app/api/auth/[...nextauth]/route.ts` - конфигурация NextAuth
- `lib/auth-utils.ts` - утилиты для хеширования
- `middleware.ts` - защита роутов
- `app/admin/login/page.tsx` - страница входа
- `scripts/generate-password.js` - генератор паролей

## 🔑 Переменные окружения:
- `ADMIN_EMAIL` - email администратора
- `ADMIN_HASHED_PASSWORD` - хеш пароля администратора
- `ADMIN_EMAILS` - разрешенные email для Google OAuth (через запятую)
- `NEXTAUTH_SECRET` - секрет для сессий
- `NEXTAUTH_URL` - URL приложения
- `GOOGLE_CLIENT_ID` - ID Google OAuth (опционально)
- `GOOGLE_CLIENT_SECRET` - секрет Google OAuth (опционально)

## 🚨 Важно для продакшена:
1. **Смените пароль** по умолчанию
2. **Используйте HTTPS**
3. **Установите сильный NEXTAUTH_SECRET**
4. **Ограничьте ADMIN_EMAILS** для OAuth
5. **Не храните .env.local в Git**

Теперь ваша админ панель полностью защищена! 🎉
