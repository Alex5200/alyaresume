# 📦 Хранение метаданных портфолио в S3

## ✅ Архитектура хранения

### S3 структура:
```
s3://bucket/
├── portfolio/
│   ├── metadata.json          ← Метаданные портфолио (heading, projects, ctaButton)
│   ├── 1771763902093_1.pdf   ← PDF файлы
│   ├── 1771763903728_2.pdf
│   └── ...
```

### Что где хранится:
- **PDF файлы** → S3 `portfolio/*.pdf`
- **Метаданные** → S3 `portfolio/metadata.json`
- **Локальный JSON** → больше не используется (можно удалить `data/portfolio.json`)

## 🛠️ Работа с метаданными

### Способ 1: Через админ панель (рекомендуется)
1. Откройте `/admin/portfolio`
2. Редактируйте проекты в интерфейсе
3. Нажмите "Сохранить"
4. Данные автоматически сохранятся в S3

### Способ 2: Через API
```bash
# Получить данные
GET /api/portfolio-s3

# Обновить данные
PUT /api/portfolio-s3
Content-Type: application/json

{
  "heading": {
    "main": "Портфолио работ",
    "description": "..."
  },
  "ctaButton": {
    "text": "Заказать проект",
    "link": "#contact"
  },
  "projects": [
    {
      "id": 1,
      "title": "Название",
      "description": "Описание",
      "pdfUrl": "https://.../portfolio/file.pdf",
      "category": "Квартиры",
      "isActive": true,
      "order": 1
    }
  ]
}
```

### Способ 3: Редактирование JSON в S3 напрямую
1. Скачайте `portfolio/metadata.json` из S3
2. Отредактируйте в текстовом редакторе
3. Загрузите обратно в S3
4. Сайт автоматически подтянет изменения

## 🔧 Добавление нового проекта

### Шаг 1: Загрузите PDF в S3
```bash
# Через админ панель /admin/portfolio
# Или через скрипт
node scripts/seed-s3-initial.js
```

### Шаг 2: Добавьте метаданные
```bash
# Получите текущие данные
curl http://localhost:3000/api/portfolio-s3 > current.json

# Отредактируйте current.json - добавьте новый проект в массив projects

# Отправьте обратно
curl -X PUT http://localhost:3000/api/portfolio-s3 \
  -H "Content-Type: application/json" \
  -d @current.json
```

### Шаг 3: Проверьте
Откройте сайт и убедитесь, что новый проект отображается.

## 📋 Структура метаданных

```json
{
  "heading": {
    "main": "Заголовок раздела",
    "description": "Описание раздела"
  },
  "ctaButton": {
    "text": "Текст кнопки",
    "link": "#contact"
  },
  "projects": [
    {
      "id": 1,                    // Уникальный ID
      "title": "Название",       // Заголовок проекта
      "description": "Описание", // Краткое описание
      "pdfUrl": "https://...",   // URL PDF файла в S3
      "category": "Квартиры",    // Категория
      "isActive": true,          // Показывать на сайте
      "order": 1                 // Порядок отображения
    }
  ]
}
```

## 🚀 Миграция данных

Если нужно перенести данные из другого источника:

```bash
# Из portfolio.json в S3
node scripts/migrate-metadata-to-s3.js
```

## ❌ Удаление старого JSON

После успешной миграции можно удалить локальный файл:

```bash
rm data/portfolio.json
```

**Важно:** Убедитесь, что данные успешно перенесены в S3 и API работает корректно перед удалением.

## 🔍 Проверка

Проверьте, что всё работает:

```bash
# Должно вернуть количество проектов (6)
curl -s http://localhost:3000/api/portfolio-s3 | jq '.projects | length'

# Должно вернуть список названий
curl -s http://localhost:3000/api/portfolio-s3 | jq '.projects[].title'

# Должно вернуть полную структуру
curl -s http://localhost:3000/api/portfolio-s3 | jq '.'
```

## 💡 Преимущества хранения в S3

1. **Централизованное хранение** - все данные в одном месте
2. **Доступ из админ панели** - редактирование через UI
3. **Резервное копирование** - S3 имеет версионирование
4. **Масштабируемость** - не зависит от файловой системы сервера
5. **Быстрый доступ** - CDN кеширование

## 📞 Поддержка

Если данные не отображаются:
1. Проверьте `portfolio/metadata.json` в S3
2. Убедитесь, что API возвращает данные: `curl /api/portfolio-s3`
3. Проверьте консоль браузера на ошибки
4. Убедитесь, что все PDF файлы доступны по URL

---

**Теперь все метаданные хранятся в S3 и редактируются через админ панель!** 🎉
