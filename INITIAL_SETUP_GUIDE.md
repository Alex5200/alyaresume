# 🚀 Начальная настройка Selectel S3

## 📋 Что будет сделано

Скрипт автоматически загрузит ваши 3 PDF файла в Selectel S3 и создаст начальные данные портфолио.

## 📁 Файлы для загрузки

Найденные PDF файлы:
1. `1.pdf` (15.6 MB) - Дизайн проект квартиры 46 кв.м
2. `2.pdf` (1.6 MB) - Дизайн проект квартиры  
3. `крузенштерна23.07.pdf` (28.7 MB) - Дизайн проект квартиры

## 🛠️ Шаги настройки

### 1. Настройте переменные окружения

Убедитесь что в `.env.local` добавлено:

```bash
# Selectel S3 Configuration
AWS_ACCESS_KEY_ID=your_selectel_access_key_id
AWS_SECRET_ACCESS_KEY=your_selectel_secret_access_key
AWS_REGION=ru-1
S3_ENDPOINT=https://s3.ru-1.selcdn.ru
S3_BUCKET_NAME=your-bucket-name
```

### 2. Создайте бакет в Selectel

1. Войдите в панель управления Selectel
2. Создайте контейнер с именем из `S3_BUCKET_NAME`
3. Включите публичный доступ для чтения
4. Настройте CORS если нужно

### 3. Проверьте подключение

```bash
node test-s3-connection.js
```

### 4. Запустите начальную загрузку

```bash
node scripts/seed-s3-initial.js
```

## 📊 Что сделает скрипт

1. **Проверит конфигурацию** S3
2. **Загрузит 3 PDF файла** в S3:
   - `portfolio/{timestamp}_1.pdf`
   - `portfolio/{timestamp}_2.pdf` 
   - `portfolio/{timestamp}_крузенштерна23.07.pdf`
3. **Создаст данные портфолио** с S3 URL
4. **Сохранит локальную копию** в `data/portfolio.json`
5. **Сгенерирует отчет** в `seed-report.json`
6. **Выведет JSON** для Edge Config

## 📝 Пример результата

```json
{
  "heading": {
    "main": "Портфолио работ",
    "description": "Профессиональные чертежи и дизайн-проекты с детальной проработкой"
  },
  "ctaButton": {
    "text": "Заказать подобный проект",
    "link": "#contact"
  },
  "projects": [
    {
      "id": 1,
      "title": "Проект квартиры на Крузенштерна",
      "description": "Полный дизайн-проект трехкомнатной квартиры с детальной проработкой всех помещений",
      "pdfUrl": "https://s3.ru-1.selcdn.ru/your-bucket/portfolio/1234567890_1.pdf",
      "category": "Квартиры",
      "isActive": true,
      "order": 1
    },
    {
      "id": 2,
      "title": "Современный интерьер", 
      "description": "Дизайн-проект гостиной с использованием современных материалов и технологий",
      "pdfUrl": "https://s3.ru-1.selcdn.ru/your-bucket/portfolio/1234567891_2.pdf",
      "category": "Интерьеры",
      "isActive": true,
      "order": 2
    },
    {
      "id": 3,
      "title": "Ремонт двухкомнатной квартиры",
      "description": "Проект реконструкции и ремонта с перепланировкой пространства", 
      "pdfUrl": "https://s3.ru-1.selcdn.ru/your-bucket/portfolio/1234567892_крузенштерна23.07.pdf",
      "category": "Ремонт",
      "isActive": true,
      "order": 3
    }
  ]
}
```

## 🔄 После загрузки

### Вариант 1: Обновить Edge Config (рекомендуется)

1. Скопируйте JSON из вывода скрипта
2. Перейдите в Vercel Dashboard → Edge Config
3. Обновите значение ключа `portfolio`

### Вариант 2: Использовать админ панель

1. Откройте `http://localhost:3000/admin/portfolio`
2. Скопируйте данные из `data/portfolio.json`
3. Вставьте и сохраните

## 🧪 Проверка работы

```bash
# Запустите dev сервер
npm run dev

# В новой терминале проверьте API
node test-api-endpoints.js
```

## 📁 Созданные файлы

- `scripts/seed-s3-initial.js` - скрипт начальной загрузки
- `seed-report.json` - отчет загрузки
- `data/portfolio.json` - локальные данные портфолио

## ⚠️ Важные моменты

- PDF файлы из `/public/pdf/` **не удаляются**
- Скрипт можно запускать многократно
- Каждый запуск создает новые файлы с уникальными timestamp
- Старые файлы в S3 остаются для совместимости

## 🆘 Если возникли проблемы

1. **Ошибка подключения**: проверьте `.env.local` и бакет в Selectel
2. **Нет доступа к файлам**: убедитесь что бакет публичный
3. **Ошибка загрузки**: проверьте размер файлов (должны быть < 100MB)
4. **API не работает**: проверьте что dev сервер запущен

## 🎯 Готово!

После выполнения этих шагов ваше портфолио будет полностью работать с Selectel S3!
