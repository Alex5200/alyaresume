# 📦 Миграция на Selectel S3

## 🎯 Что было сделано

Обновлена существующая S3 инфраструктура для работы с Selectel S3 хранилищем.

## 📁 Измененные файлы

### 1. `lib/s3.ts` - S3 клиент для Selectel
- Добавлен endpoint Selectel: `https://s3.ru-1.selcdn.ru`
- Изменен регион на `ru-1`
- Добавлен `forcePathStyle: true` для совместимости
- Обновлена генерация URL для Selectel

### 2. `app/api/s3-upload/route.ts` - API для загрузки
- Обновлена генерация URL в GET методе

### 3. `app/api/portfolio-s3/route.ts` - API для данных
- Обновлены URL в DEFAULT_DATA для Selectel

### 4. `scripts/migrate-to-s3.js` - Скрипт миграции
- Добавлена конфигурация Selectel
- Обновлена генерация URL

## 🚀 Шаги для настройки

### 1. Настройте Selectel credentials

Добавьте в `.env.local`:

```bash
# Selectel S3 Configuration
AWS_ACCESS_KEY_ID=your_selectel_access_key_id
AWS_SECRET_ACCESS_KEY=your_selectel_secret_access_key
AWS_REGION=ru-1
S3_ENDPOINT=https://s3.ru-1.selcdn.ru
S3_BUCKET_NAME=your-bucket-name
```

### 2. Создайте бакет в Selectel

1. В панели управления Selectel создайте контейнер
2. Укажите имя из `S3_BUCKET_NAME`
3. Включите публичный доступ (если нужны прямые ссылки)
4. Настройте CORS при необходимости

### 3. Запустите миграцию

```bash
node scripts/migrate-to-s3.js
```

### 4. Проверьте работу

- Загрузка файлов: `POST /api/s3-upload`
- Список файлов: `GET /api/s3-upload`
- Данные портфолио: `GET /api/portfolio-s3`

## 🔧 Selectel регионы

- `ru-1`: Москва - `https://s3.ru-1.selcdn.ru`
- `ru-2`: Москва - `https://s3.ru-2.selcdn.ru`
- `ru-3`: Санкт-Петербург - `https://s3.ru-3.selcdn.ru`
- `ru-4`: Казань - `https://s3.ru-4.selcdn.ru`
- `ru-5`: Новосибирск - `https://s3.ru-5.selcdn.ru`
- `ru-7`: Екатеринбург - `https://s3.ru-7.selcdn.ru`

## 📊 Структура в Selectel S3

```
your-bucket/
└── portfolio/
    ├── 1234567890_file1.pdf
    ├── 1234567891_file2.pdf
    └── ...
```

## 🔗 URL формат

Для Selectel используется path-style URL:
```
https://s3.ru-1.selcdn.ru/your-bucket-name/portfolio/file.pdf
```

## ⚠️ Важные отличия от AWS S3

1. **Endpoint**: Используется endpoint Selectel, а не AWS
2. **Регион**: Другие значения регионов (ru-1, ru-2, etc.)
3. **URL формат**: Path-style вместо virtual-hosted style
4. **forcePathStyle**: Обязательно设置为 true

## 🛡️ Безопасность

- Файлы хранятся с публичным доступом (ACL: public-read)
- Selectel credentials в `.env.local`
- Валидация типа файла (только PDF)
- Лимит размера (10MB)
