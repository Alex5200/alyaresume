# 📦 Миграция на AWS S3

## 🎯 Что было сделано

Создана полная инфраструктура для хранения PDF и данных в AWS S3 вместо локального хранилища.

## 📁 Созданные файлы

### 1. `lib/s3.ts` - S3 клиент
- Конфигурация AWS S3 клиента
- Функции: `uploadToS3`, `deleteFromS3`, `listS3Files`, `getSignedUploadUrl`

### 2. `app/api/s3-upload/route.ts` - API для загрузки
- POST - загрузка файлов в S3
- GET - список файлов из S3
- DELETE - удаление файлов из S3

### 3. `app/api/portfolio-s3/route.ts` - API для данных
- Чтение портфолио с S3 URL
- Обновление данных в Edge Config

### 4. `scripts/migrate-to-s3.js` - Скрипт миграции
- Переносит все PDF из `/public/pdf/` в S3
- Создает отчет миграции
- Генерирует обновленные данные портфолио

## 🚀 Шаги для миграции

### 1. Настройте AWS credentials

Добавьте в `.env.local`:

```bash
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-bucket-name
```

### 2. Создайте S3 бакет

В AWS Console:
1. Создайте бакет с именем из `S3_BUCKET_NAME`
2. Включите публичный доступ (если нужны прямые ссылки)
3. Настройте CORS для доступа с вашего домена

### 3. Запустите миграцию

```bash
node scripts/migrate-to-s3.js
```

Скрипт:
- Загрузит все PDF в S3
- Создаст `migration-report.json` с результатами
- Выведет обновленные данные для портфолио

### 4. Обновите данные портфолио

Скопируйте вывод из скрипта и обновите:
- Локально: `data/portfolio.json`
- Или в Edge Config через админ панель

### 5. Обновите компоненты

Замените использование `/api/upload` на `/api/s3-upload` в компонентах загрузки.

## 🔧 Использование в коде

### Загрузка файла в S3:
```typescript
const formData = new FormData();
formData.append('file', pdfFile);

const response = await fetch('/api/s3-upload', {
  method: 'POST',
  body: formData
});

const { url, key } = await response.json();
// url - публичный URL файла в S3
```

### Получение списка файлов:
```typescript
const response = await fetch('/api/s3-upload');
const { files } = await response.json();
```

### Удаление файла:
```typescript
await fetch('/api/s3-upload', {
  method: 'DELETE',
  body: JSON.stringify({ key: 'portfolio/file.pdf' })
});
```

## 🛡️ Безопасность

- Файлы хранятся в S3 с публичным доступом (для прямых ссылок)
- Или используйте подписанные URL для приватного доступа
- AWS credentials хранятся в `.env.local` (не в Git)
- Проверка типа файла (только PDF)
- Лимит размера (10MB)

## 📊 Структура в S3

```
your-bucket/
└── portfolio/
    ├── 1234567890_file1.pdf
    ├── 1234567891_file2.pdf
    └── ...
```

## 🔄 Откат изменений

Если нужно вернуться к локальному хранилищу:
1. PDF остаются в `/public/pdf/` (не удаляются при миграции)
2. Переключите API обратно на `/api/upload` (Vercel Blob)
3. Или используйте локальные пути `/pdf/filename.pdf`

## 📝 Примечания

- Старые файлы в `/public/pdf/` не удаляются автоматически
- Миграцию можно запускать многократно (создаст дубликаты с новыми именами)
- Отчет сохраняется в `migration-report.json`

## 💡 Советы

1. **Для production**: используйте IAM роли вместо access keys
2. **CDN**: настройте CloudFront перед S3 для кэширования
3. **Бэкап**: включите versioning в S3 бакете
4. **Мониторинг**: настройте CloudWatch для отслеживания использования
